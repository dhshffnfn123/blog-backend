let Post;_299‍.x([["write",()=>write],["list",()=>list],["read",()=>read],["remove",()=>remove],["update",()=>update]]);_299‍.w("../../models/post",[["default",["Post"],function(v){Post=v}]]);

// POST /api/posts { title : 제목, body : 내용, tags [태그1, 태그2]}
       const write = async ctx => {
    const { title, body, tags } = ctx.request.body;
    const post = new Post ({
        title,
        body,
        tags,
    });
    try {
        await post.save(); // save 함수가 실행되면 DB에 저장 , await을 사용할 때는 try/catch로 오류를 처리해야한다.
        ctx.body = post;
    } catch (e) {
        ctx.throw(500, e);
    }
};

       const list = ctx => {};

       const read = ctx => {};

       const remove = ctx => {};

       const update = ctx => {};